name: vinted_lens
description: "Application de recherche visuelle pour vêtements"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Dépendances essentielles seulement
  http: ^1.1.0               # API calls
  image_picker: ^1.0.4       # Photo selection
  cupertino_icons: ^1.0.2    # Icons

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true